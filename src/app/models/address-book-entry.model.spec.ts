import { AddressBookEntry } from './address-book-entry.model';

describe('AddressBookEntry', () => {
  it('should create an instance', () => {
    expect(new AddressBookEntry()).toBeTruthy();
  });
});
